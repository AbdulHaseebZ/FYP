
I am running a Home Automation company where i Integrate Inverters with my system. However to do so, I am facing a challenge that there are many white labelled inverters and it is hard to find which model they are an thus hard to find which modbus registers (the addresses) contain the values  of important real time parameters. However, I have seen that if I fetch the log file of all the registers, images from the inverter of actual data, i can run some analysis and find all the registers. However it is important to go through the complete log file and return all the possibilities. I have attached the LOG file of one of our inverters and the images from the inverter which shows the real time data. Run a full depth analysis and figure out if any of these values are present in the log? Which registers are those? Also you need to understand that log files and images data can be slightly different due to the time difference of log capturing and image capturing. Keep that in mind. also keep in mind that all voltages have scaling factor of 0.1 except battery voltages which may have 0.1 or 0.01, Currents are mostly 0.01 but some inverter may have some currents with 0.1 scaling factor.Also you dont have to be 100% accurate. You can also give me multiple registers that you think are close by. However, I need all the register information that are present in the images attached. Don't write anything else in your response just return me a json text showing all the parameters with their possible addresses (all those register addresses where that real time parameter value or close to that value is found from the log file) also show the values present at those addresses.